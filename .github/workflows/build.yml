name: build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

env:
  REGISTRY: ghcr.io
  IMAGE_OWNER: ${{ github.repository_owner }}

jobs:
  quality:
    name: quality gates
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20.18.0'
          cache: 'npm'

      - name: Install JS deps
        run: |
          rm -rf node_modules
          npm ci --workspaces --include-workspace-root || echo "WARN: npm ci exited non-zero (npm cli #4828)"

      - name: Install Linux native bindings
        run: |
          set +e
          echo "--- BEFORE ---"
          ls node_modules/@rolldown/ 2>&1 || echo MISSING_ROOT
          ls node_modules/@oxc-parser/ 2>&1 || echo MISSING_ROOT
          npm install --no-save --no-package-lock --ignore-scripts --force \
            @rolldown/binding-linux-x64-gnu@1.2.6 \
            @oxc-parser/binding-linux-x64-gnu@0.143.0
          echo "--- AFTER npm install ---"
          ls node_modules/@rolldown/ 2>&1 || echo MISSING_AFTER
          ls node_modules/@oxc-parser/ 2>&1 || echo MISSING_AFTER
          echo "--- rolldown resolve path ---"
          node -e "try{console.log(require.resolve('rolldown'))}catch(e){console.log('NO_RESOLVE:',e.message)}"
          find node_modules -maxdepth 4 -name "rolldown-binding.linux-x64-gnu.node" 2>&1 || echo NO_BINARY
          echo "--- end ---"

      - name: Typecheck client
        run: npm run typecheck:client

      - name: Build admin (compile + typecheck gate)
        run: npm run build:admin

      - name: Build client
        run: npm run build:client

      - name: Build gateway
        run: npm run build:gateway

  docker:
    name: docker build (${{ matrix.image }})
    needs: quality
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - image: server
            dockerfile: docker/Dockerfile.server
            platforms: linux/amd64
          - image: gateway
            dockerfile: docker/Dockerfile.gateway
            platforms: linux/amd64,linux/arm64
          - image: admin
            dockerfile: docker/Dockerfile.web
            build-args: APP=mudadmin
            platforms: linux/amd64,linux/arm64
          - image: client
            dockerfile: docker/Dockerfile.web
            build-args: APP=mudclient,WS_URL=wss://CHANGE_ME/ws
            platforms: linux/amd64,linux/arm64
    steps:
      - uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_OWNER }}/mud-${{ matrix.image }}
          tags: |
            type=sha,prefix=sha-
            type=raw,value=latest,enable={{is_default_branch}}
            type=raw,value=prod,enable={{is_default_branch}}
            type=raw,value=beta,enable={{is_default_branch}}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          platforms: ${{ matrix.platforms }}
          push: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: ${{ matrix.build-args }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
